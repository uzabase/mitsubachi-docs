# select-menu-item

select-menu-item は、選択状態（チェックマーク）を持つメニューアイテムです。一覧の中から「現在どれが選択されているか」を視覚的に伝えます。

## 使いどころと選び方

### 使うべきシーン
- 言語切り替えのように、選択中の項目にチェックマークを表示したいとき
- フィルタ設定や表示切り替えなど、現在の状態を一覧内で示したいとき
- アクションの結果としてシステム状態が変わるが、トリガーのラベルは固定のとき

### 使わないほうがよいシーン
- 選択した値をトリガーに表示・保持したい場合（→ Select を使います）
- 選択状態の表示が不要なアクション（→ [action-menu-item](./action-menu-item.md) を使います）

### 他コンポーネントとの違い・使い分け
- **[action-menu-item](./action-menu-item.md) との違い**：select-menu-item はチェックマークによって選択状態を示す点が異なります。「現在この設定が有効である」ことを示すために使います。

## Figma

- コンポーネント: https://www.figma.com/design/kHQNLM1dnk0EhZwOKBEBkL/Base-Component-Speeda-3.1-MITSUBACHI?node-id=8376-4776

> mockup で再現する場合は `mockup/mitsubachi-mockup.css` の `.mi-menu-item`（選択中は `--selected`） を使う（自作しない）。

## 構成とルール

### バリエーション・状態

#### Selected
- `false`：未選択。
- `true`：選択済み。チェックマークを表示します。

#### State
- `default`：通常の状態。
- `hover`：マウスカーソルが重なっている状態。オーバーレイで視覚フィードバックを表示します。
- `active`：クリック・タップ中の状態。
- `focus`：フォーカスされている状態。フォーカスリングで視覚化します。
- `disabled`：操作不可の状態。グレーアウトで表示されます。

#### Show icon
- アイコンの有無を切り替えられます（任意）

### コンテンツルール
- ラベルは選択対象を端的に表す名詞または名詞句で記述します
- ラベルは1行に収まる長さとします

## 振る舞い

- クリック（タップ）で選択状態が切り替わります。選択後、menu は閉じます
- hover 時に背景色が変化します
- キーボードの上下矢印キーでフォーカスを移動できます
- Enter または Space で選択が確定します
- disabled 状態ではクリック・キーボード操作どちらも無効となります

## Do

- 現在の選択状態をチェックマークで示す用途に使う
- ラジオボタン型のメニュー（単一選択）として使う

## Don't

- 選択状態の表示が不要なアクションに使わない → [action-menu-item](./action-menu-item.md) を使う
- 選択した値をトリガーに表示・保持したい場合には使わない → Select を使う

## 役割と目的
- 一覧の中から現在選択されている項目をチェックマークで示します
- ラジオボタン型のメニュー（単一選択）として使います
- [menu](./index.md) コンポーネント内で使うアイテムとして機能します
