# ub-logo

Uzabase のロゴを表示するコンポーネントです。フッターなど親会社ブランドを明示する場面で使用します。

## 使いどころと選び方

### 使うべきシーン
- フッターなど親会社 Uzabase のブランドを表示する場面
- 暗い背景に白抜きロゴを表示する場面（inverse）

### 使わないほうがよいシーン
- Speeda ブランドのロゴを表示する場合 → [sp-logo](./sp-logo.md)
- シンボルマークのみで十分な場合 → [sp-symbol](./sp-symbol.md)

## Figma

<!-- TODO: コンポーネントのnode-idを確認 -->

> mockup で再現する場合は `mockup/mitsubachi-logos.css` を追加読み込みして `.mi-logo--uzabase` を使う（自作しない）。

## 構成とルール

### バリエーション・状態

| プロパティ | 値 | 説明 |
|-----------|-----|------|
| inverse | false / true | 明るい背景用（通常）/ 暗い背景用（白抜き） |

### コンテンツルール
- ロゴのサイズは用途に応じて調整可能だが、アスペクト比は維持する
- 最小サイズを下回らないようにする（視認性確保）

## Do
- ロゴの周囲には十分な余白（クリアスペース）を確保する
- 暗い背景では `inverse=true` を使用する

## Don't
- ロゴのアスペクト比を変えない
- 他の要素とロゴを密着させない
- 最小サイズを下回るサイズで使用しない
