# コンポーネントドキュメント テンプレート

> 新しいコンポーネントのドキュメントを書くときは、このテンプレートをコピーしてください。
>
> **書く前に確認すること:**
> - Figma のコンポーネントフレームを見て、variant / size / state / selected などのプロパティを洗い出す
> - **同一ページ内に複数のコンポーネントがあるか確認する**（例: `link-tag` と `read-only-tag`）
> - Figma ページ内の「仕様」フレームで Do/Don't を確認する（仕様フレームがないコンポーネントもある。その場合は Do/Don't もドキュメントに書く）
> - Storybook も確認し、使い分けやアクセシビリティのヒントを探す
>
> **書き方の方針:**
> - Figma MCP で読み取れるものはドキュメントに書かない:
>   - バリアント・サイズ・色・寸法・影・角丸などの視覚情報 → コンポーネントフレームから取得可能
>   - Do / Don't → 「仕様」フレームから取得可能
> - Figma MCP では読み取れないものだけをドキュメントで補う:
>   - **コンポーネント一覧** — 同一ページ内に複数コンポーネントがある場合、それぞれの役割
>   - **使い分け** — variant は見えるが「いつどれを選ぶか」の判断基準
>   - **選択状態の対応表** — どの variant で selected が使えるかのルール
>   - **アクセシビリティ** — aria 属性、キーボード操作
>   - **並べ方** — 複数コンポーネント間の配置順序・間隔
> - 該当しないセクションは削除してよい
> - `{...}` の部分を実際の内容に置き換える
> - **`_` で始まるコンポーネントは Figma 内部用なので、ドキュメントには含めない**

---

<!--
  ======== セクションの並び順について ========
  この順番は固定する。Claude がプロトタイプ生成時に読み込むため、
  「判断 → 描画 → 詳細 → 制約」の順で前方に重要情報を置く。
  全コンポーネントの md で同じ並びを維持すること（agent が構造を予測でき、読み込み効率が上がる）。

  1. 1行サマリ        … 何か（一目で対象を把握）
  2. 使いどころと選び方  … 使ってよいか判断（選定ゲートを最初に効かせる）
  3. Figma            … どう描画するか（mockup クラス・MCP）。出力の手段なので上位
  4. 構成とルール       … どの variant / state か
  5. コンテンツルール    … 組み立て・配置の制約
  6. 振る舞い          … インタラクション（動くプロトタイプ用）
  7. Do / Don't       … 生成直前のガードレール
  8. 役割と目的（任意）  … 背景・rationale。生成判断に最も使わないので末尾。
                        1行サマリで足りるなら省略してよい

  該当しないセクションは削除してよい。{...} を実際の内容に置き換える。
-->

<!-- ======== ここからコピー ======== -->

# {コンポーネント名}

{このコンポーネントが何なのか、1 行で説明する。}

## 使いどころと選び方

<!--
  選定ゲート。まず「使ってよいか／別コンポーネントに振り分けるべきか」を判断させる。
  同一 md に複数コンポーネントがある場合は、冒頭にコンポーネント一覧表を置く:

  | コンポーネント | 役割 | いつ使うか |
  |---------------|------|-----------|
  | **{名前}** | {何をするものか} | {選ぶ場面} |
-->

### 使うべきシーン
- {使う場面}

### 使わないほうがよいシーン
- {使わない場面（代替コンポーネントへのリンクを添える）}

### 他コンポーネントとの違い・使い分け
<!-- 似たコンポーネントとの違いを表で示す。1つしかない場合は削除してよい。 -->

## Figma
- コンポーネント: {Figma のコンポーネントフレームへの URL}
- 各variantの値は Figma MCP（`get_design_context`）で取得

<!--
  mockup で再現する場合に使う kit クラスを明記する（自作させない）。例:
  > mockup で再現する場合は `mockup/mitsubachi-mockup.css` の `.mi-{...}` を使う（自作しない）。

  Do / Don't は Figma ページ内の「仕様」フレームから取得可能。
  仕様フレームがない場合は末尾の Do / Don't セクションで補う。
-->

## 構成とルール

<!--
  variant / size / state / selected を整理する。
  Figma MCP は variant の存在は読めるが「いつどれを選ぶか」は読めないので、
  使い分けの判断基準を書く。具体値（色・寸法）は md にハードコードしない。
-->

### バリエーション・状態

| プロパティ | 値 |
|---|---|
| {プロパティ名} | `{値}` / `{値}` |

#### {variant の種類}
- `{値}`: {いつ選ぶか}

<!-- selected がある場合は、どの variant で使えるかの対応表を書く。 -->

### コンテンツルール
- {テキスト・ラベル・配置のルール}

## 振る舞い

<!-- クリック／タップ・フォーカス・キーボード操作・disabled などのインタラクション。 -->

- {振る舞い}

## Do
- {やるべきこと}

## Don't
- {やってはいけないこと}

## 役割と目的

<!--
  任意・末尾。背景や解決する課題などの rationale。
  1行サマリで足りる場合はセクションごと削除してよい。
-->

- **解決する課題**: {何を解決するか}
- **UI上での基本的な役割**: {役割}
- **ユーザーにとっての意味**: {意味}
